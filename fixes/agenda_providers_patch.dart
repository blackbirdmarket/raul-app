// PATCH — agenda_providers.dart
//
// Agrega la propiedad `icon` al enum PlanningView.
// Reemplaza el enum existente en:
//   lib/features/planning/presentation/providers/agenda_providers.dart
//
// 1. Agrega al inicio del archivo:
//    import 'package:flutter/material.dart';
//
// 2. Reemplaza el enum PlanningView por:

/*
enum PlanningView {
  day('Dia', Icons.view_day_rounded),
  agenda('Agenda', Icons.view_agenda_rounded);

  const PlanningView(this.label, this.icon);

  final String label;
  final IconData icon;
}
*/
//
// El resto del archivo queda igual.
